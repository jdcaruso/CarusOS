#pragma once

// CarusOS secrets - credentials and private endpoints.
//
//   1. Copy this file to "secrets.h" (same folder).
//   2. Fill in your own values below.
//
// secrets.h is listed in .gitignore and must NEVER be committed.

// WiFi network the device connects to
#define WIFI_SSID               "YOUR_WIFI_SSID"
#define WIFI_PASSWORD           "YOUR_WIFI_PASSWORD"

// OTA (Over-The-Air) update password
#define CARUSOS_OTA_PASSWORD    "changeme"
